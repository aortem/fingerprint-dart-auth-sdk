diff --git a/.gitlab-ci.yml b/.gitlab-ci.yml
index 7f5fd48..ab99fe9 100644
--- a/.gitlab-ci.yml
+++ b/.gitlab-ci.yml
@@ -44,6 +44,13 @@ debug_merge_request:
     - when: never
 
 # Validation Conditional Triggers for Docs
+validation_trigger:
+  stage: validation
+  trigger:
+    include: tools/pipelines/backend/child-ci-setup-validation.yml
+  rules:
+    - when: always
+
 validation_trigger_docs:
   stage: validation
   trigger:
diff --git a/fingerprint-dart-auth-sdk/CHANGELOG.md b/fingerprint-dart-auth-sdk/CHANGELOG.md
index 64b53c8..9b02921 100644
--- a/fingerprint-dart-auth-sdk/CHANGELOG.md
+++ b/fingerprint-dart-auth-sdk/CHANGELOG.md
@@ -1,5 +1,22 @@
 # Changelog
 
+## [0.0.4]
+### Updated
+
+* Raised the package baseline to Dart `^3.11.4`.
+* Upgraded direct package dependencies to current compatible releases:
+  * `build_web_compilers` to `^4.4.17`
+  * `crypto` to `^3.0.7`
+  * `ds_standard_features` to `^0.1.6`
+  * `jwt_generator` to `^0.0.9`
+
+### Fixed
+
+* Added injectable HTTP client support to `FingerprintAuth` for deterministic tests.
+* Aligned webhook-signature verification with the shipped examples using HMAC-SHA256.
+* Restored the backend validation child pipeline and corrected release changelog parsing.
+* Removed the stray `docs/test.txt` artifact from the publishable package.
+
 ## [0.0.3]
 ### Updated
 
diff --git a/fingerprint-dart-auth-sdk/README.md b/fingerprint-dart-auth-sdk/README.md
index 7310e4b..157214f 100644
--- a/fingerprint-dart-auth-sdk/README.md
+++ b/fingerprint-dart-auth-sdk/README.md
@@ -79,7 +79,7 @@ Or manually in your `pubspec.yaml`:
 
 ```yaml
 dependencies:
-  fingerprint_dart_auth_sdk: ^0.0.2
+  fingerprint_dart_auth_sdk: ^0.0.4
 ```
 
 Then fetch:
diff --git a/fingerprint-dart-auth-sdk/docs/components/fingerprint-dart-auth-sdk/modules/ROOT/pages/releases.adoc b/fingerprint-dart-auth-sdk/docs/components/fingerprint-dart-auth-sdk/modules/ROOT/pages/releases.adoc
index 4e8e084..b960d3d 100644
--- a/fingerprint-dart-auth-sdk/docs/components/fingerprint-dart-auth-sdk/modules/ROOT/pages/releases.adoc
+++ b/fingerprint-dart-auth-sdk/docs/components/fingerprint-dart-auth-sdk/modules/ROOT/pages/releases.adoc
@@ -11,6 +11,21 @@ This page summarizes the stable package release history for Fingerprint Dart Aut
 * Pubspec: apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/pubspec.yaml
 * Changelog: apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/CHANGELOG.md
 
+== v0.0.4
+
+=== Overview
+This release finalizes the Dart 3.11.4 baseline, restores test coverage, and aligns webhook-signature verification with the shipped examples.
+
+=== Highlights
+* Dart SDK baseline aligned to `^3.11.4`
+* Webhook-signature verification hardened with HMAC-SHA256
+* Validation and release-pipeline fixes
+
+=== Improvements
+* Upgraded direct package dependencies to current compatible releases.
+* Added injectable HTTP client support to `FingerprintAuth` for reliable testing.
+* Removed the stray docs artifact from the publishable package.
+
 == v0.0.3
 
 === Overview
diff --git a/fingerprint-dart-auth-sdk/docs/test.txt b/fingerprint-dart-auth-sdk/docs/test.txt
deleted file mode 100644
index 9a2c773..0000000
--- a/fingerprint-dart-auth-sdk/docs/test.txt
+++ /dev/null
@@ -1 +0,0 @@
-testing
\ No newline at end of file
diff --git a/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-dart-web-app/pubspec.yaml b/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-dart-web-app/pubspec.yaml
index b9aa457..cd3aef3 100644
--- a/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-dart-web-app/pubspec.yaml
+++ b/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-dart-web-app/pubspec.yaml
@@ -5,7 +5,7 @@ version: 1.0.0
 publish_to: none
 
 environment:
-  sdk: ^3.11.0
+  sdk: ^3.11.4
 
 # Add regular dependencies here.
 dependencies:
@@ -14,6 +14,6 @@ dependencies:
     path: ../../../fingerprint-dart-auth-sdk
 
 dev_dependencies:
-  build_runner: ^2.4.15
-  build_web_compilers: ^4.1.6
+  build_runner: ^2.10.5
+  build_web_compilers: ^4.4.17
   lints: ^6.0.0
diff --git a/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-mobile-app/pubspec.yaml b/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-mobile-app/pubspec.yaml
index 3488a28..e814455 100644
--- a/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-mobile-app/pubspec.yaml
+++ b/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-mobile-app/pubspec.yaml
@@ -19,7 +19,7 @@ publish_to: "none" # Remove this line if you wish to publish to pub.dev
 version: 0.0.1
 
 environment:
-  sdk: ^3.11.0
+  sdk: ^3.11.4
 
 # Dependencies specify other packages that your package needs in order to work.
 # To automatically upgrade your package dependencies to the latest versions
diff --git a/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-web-app/pubspec.yaml b/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-web-app/pubspec.yaml
index 7302f15..1ee7c78 100644
--- a/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-web-app/pubspec.yaml
+++ b/fingerprint-dart-auth-sdk/example/fingerprint-dart-auth-sdk-flutter-web-app/pubspec.yaml
@@ -4,7 +4,7 @@ publish_to: 'none'
 version: 0.1.0
 
 environment:
-  sdk: ^3.11.0
+  sdk: ^3.11.4
 
 dependencies:
   flutter:
diff --git a/fingerprint-dart-auth-sdk/lib/src/core/fingerprint_sdk_setup.dart b/fingerprint-dart-auth-sdk/lib/src/core/fingerprint_sdk_setup.dart
index 9d22705..de5c910 100644
--- a/fingerprint-dart-auth-sdk/lib/src/core/fingerprint_sdk_setup.dart
+++ b/fingerprint-dart-auth-sdk/lib/src/core/fingerprint_sdk_setup.dart
@@ -1,5 +1,5 @@
-// import 'dart:io';
 import 'dart:convert';
+
 import 'package:ds_standard_features/ds_standard_features.dart' as http;
 
 /// FingerprintAuth handles fingerprint authentication using the FingerprintJS API.
@@ -11,19 +11,19 @@ class FingerprintAuth {
   /// Base URL for the FingerprintJS API.
   final String baseUrl;
 
-  /// Constructor to initialize API key and base URL.
+  /// HTTP client used for verification requests.
+  final http.Client client;
+
+  /// Constructor to initialize API key, base URL, and optional client.
   ///
-  /// The API key can be provided directly, via an environment variable,
-  /// or by setting the `FINGERPRINTJS_API_KEY` in the system environment.
+  /// The API key can be provided directly or via an environment variable.
   FingerprintAuth({
     String? apiKey,
     String? envVar,
     this.baseUrl = 'https://api.fingerprintjs.com',
-  }) : apiKey =
-           apiKey ??
-           envVar ??
-           //  Platform.environment['FINGERPRINTJS_API_KEY'] ??
-           '' {
+    http.Client? client,
+  }) : apiKey = apiKey ?? envVar ?? '',
+       client = client ?? http.Client() {
     if (this.apiKey.isEmpty) {
       throw ArgumentError(
         'API key must be provided either directly or via environment variables.',
@@ -37,10 +37,10 @@ class FingerprintAuth {
   /// Throws an exception if the verification request fails.
   Future<Map<String, dynamic>> verify(String payload) async {
     final url = Uri.parse('$baseUrl/verify');
-    final response = await http.post(
+    final response = await client.post(
       url,
       headers: {
-        'Authorization': 'Bearer $apiKey', // ✅ Fixed string interpolation
+        'Authorization': 'Bearer $apiKey',
         'Content-Type': 'application/json',
       },
       body: payload,
@@ -50,8 +50,8 @@ class FingerprintAuth {
       return response.body.isNotEmpty
           ? jsonDecode(response.body) as Map<String, dynamic>
           : {};
-    } else {
-      throw Exception('Verification failed: ${response.body}');
     }
+
+    throw Exception('Verification failed: ${response.body}');
   }
 }
diff --git a/fingerprint-dart-auth-sdk/lib/src/utils/fingerprint_is_valid_webhook_signature.dart b/fingerprint-dart-auth-sdk/lib/src/utils/fingerprint_is_valid_webhook_signature.dart
index 6958f14..9b1d4a8 100644
--- a/fingerprint-dart-auth-sdk/lib/src/utils/fingerprint_is_valid_webhook_signature.dart
+++ b/fingerprint-dart-auth-sdk/lib/src/utils/fingerprint_is_valid_webhook_signature.dart
@@ -1,12 +1,12 @@
 import 'dart:convert';
-import 'package:jwt_generator/jwt_generator.dart';
 
-/// Verifies the authenticity of an incoming webhook request using
-/// HMAC-SHA256 via the `jwt_generator` package.
+import 'package:crypto/crypto.dart';
+
+/// Verifies the authenticity of an incoming webhook request using HMAC-SHA256.
 ///
-/// - [payload]: Raw webhook body (String)
-/// - [signature]: Base64-encoded signature from the request
-/// - [secret]: Secret key shared between client and server
+/// - [payload]: Raw webhook body.
+/// - [signature]: Hex-encoded HMAC digest from the request.
+/// - [secret]: Secret key shared between client and server.
 bool isValidWebhookSignature({
   required String payload,
   required String signature,
@@ -22,22 +22,20 @@ bool isValidWebhookSignature({
     throw ArgumentError('Secret key cannot be empty.');
   }
 
-  // 1. Convert secret to bytes (expected by constructor)
-  final secretBytes = utf8.encode(secret);
-  final verifier = HmacSignatureVerifier(secret: secretBytes);
+  final hmac = Hmac(sha256, utf8.encode(secret));
+  final digest = hmac.convert(utf8.encode(payload)).toString();
 
-  // 2. Pass payload as String and signature as String to verify()
-  String normalizedSignature;
-  try {
-    // Ensure signature is valid Base64
-    base64Decode(signature);
-    normalizedSignature = signature;
-  } catch (_) {
-    throw ArgumentError('Signature must be valid Base64.');
-  }
+  return _secureCompare(digest, signature);
+}
 
-  // 3. Perform verification
-  final isValid = verifier.verify(payload, normalizedSignature);
+bool _secureCompare(String a, String b) {
+  if (a.length != b.length) {
+    return false;
+  }
 
-  return isValid;
+  var result = 0;
+  for (var i = 0; i < a.length; i++) {
+    result |= a.codeUnitAt(i) ^ b.codeUnitAt(i);
+  }
+  return result == 0;
 }
diff --git a/fingerprint-dart-auth-sdk/pubspec.yaml b/fingerprint-dart-auth-sdk/pubspec.yaml
index e8e37a5..bd4cc22 100644
--- a/fingerprint-dart-auth-sdk/pubspec.yaml
+++ b/fingerprint-dart-auth-sdk/pubspec.yaml
@@ -1,11 +1,11 @@
 name: fingerprint_dart_auth_sdk
 description: A Dart SDK for Fingerprint Authentication, enabling secure server-side authentication and user management for Dart-based backend applications.
-version: 0.0.3
+version: 0.0.4
 repository: https://github.com/aortem/fingerprint-dart-auth-sdk
 license: BSD-3
 
 environment:
-  sdk: ^3.11.0
+  sdk: ^3.11.4
 
 # Add regular dependencies here.
 
@@ -13,9 +13,10 @@ dependencies:
 #------------------ STANDARD FEATURES----------------------dart-----------
 ## There are three standard libraries.  Each one can be used with a different xxx 
 
-  ds_standard_features: ^0.1.5
-  build_web_compilers: ^4.3.0
-  jwt_generator: ^0.0.8
+  build_web_compilers: ^4.4.17
+  crypto: ^3.0.7
+  ds_standard_features: ^0.1.6
+  jwt_generator: ^0.0.9
   
 
 
diff --git a/fingerprint-dart-auth-sdk/test/support/fake_http_client.dart b/fingerprint-dart-auth-sdk/test/support/fake_http_client.dart
new file mode 100644
index 0000000..518e6b4
--- /dev/null
+++ b/fingerprint-dart-auth-sdk/test/support/fake_http_client.dart
@@ -0,0 +1,23 @@
+import 'dart:async';
+
+import 'package:ds_standard_features/ds_standard_features.dart' as http;
+
+typedef ResponseHandler = Future<http.Response> Function(http.BaseRequest request);
+
+class FakeHttpClient extends http.BaseClient {
+  FakeHttpClient(this._handler);
+
+  final ResponseHandler _handler;
+
+  @override
+  Future<http.StreamedResponse> send(http.BaseRequest request) async {
+    final response = await _handler(request);
+    return http.StreamedResponse(
+      Stream<List<int>>.value(response.bodyBytes),
+      response.statusCode,
+      headers: response.headers,
+      reasonPhrase: response.reasonPhrase,
+      request: request,
+    );
+  }
+}
diff --git a/fingerprint-dart-auth-sdk/test/unit/core/fingerprint_sdk_setup_test.dart b/fingerprint-dart-auth-sdk/test/unit/core/fingerprint_sdk_setup_test.dart
index ab54860..58400ea 100644
--- a/fingerprint-dart-auth-sdk/test/unit/core/fingerprint_sdk_setup_test.dart
+++ b/fingerprint-dart-auth-sdk/test/unit/core/fingerprint_sdk_setup_test.dart
@@ -1,39 +1,37 @@
+import 'package:ds_standard_features/ds_standard_features.dart' as http;
 import 'package:ds_tools_testing/ds_tools_testing.dart';
 import 'package:fingerprint_dart_auth_sdk/src/core/fingerprint_sdk_setup.dart';
-
-import 'package:ds_standard_features/ds_standard_features.dart' as http;
-
-class MockHttpClient extends Mock implements http.Client {}
+import '../../support/fake_http_client.dart';
 
 void main() {
   group('FingerprintAuth', () {
-    late MockHttpClient mockHttpClient;
-    late FingerprintAuth auth;
-
-    setUp(() {
-      mockHttpClient = MockHttpClient();
-      auth = FingerprintAuth(apiKey: 'test_api_key');
-    });
-
-    test('should throw ArgumentError if API key is not provided', () {
+    test('throws ArgumentError if API key is not provided', () {
       expect(() => FingerprintAuth(apiKey: ''), throwsArgumentError);
     });
 
-    test('should throw Exception on verification failure', () async {
-      final url = Uri.parse('${auth.baseUrl}/verify');
-
-      when(
-        () => mockHttpClient.post(
-          url,
-          headers: any(named: 'headers'),
-          body: any(named: 'body'),
-        ),
-      ).thenAnswer((_) async => http.Response('Error', 400));
+    test('throws Exception on verification failure', () async {
+      final auth = FingerprintAuth(
+        apiKey: 'test_api_key',
+        client: FakeHttpClient((_) async => http.Response('Error', 400)),
+      );
 
       expect(
         () => auth.verify('{"fingerprint": "test_data"}'),
         throwsA(isA<Exception>()),
       );
     });
+
+    test('returns decoded payload on successful verification', () async {
+      final auth = FingerprintAuth(
+        apiKey: 'test_api_key',
+        client: FakeHttpClient(
+          (_) async => http.Response('{"verified":true}', 200),
+        ),
+      );
+
+      final result = await auth.verify('{"fingerprint":"test_data"}');
+
+      expect(result['verified'], true);
+    });
   });
 }
diff --git a/fingerprint-dart-auth-sdk/test/unit/utils/fingerprint_is_valid_webhook_signature_test.dart b/fingerprint-dart-auth-sdk/test/unit/utils/fingerprint_is_valid_webhook_signature_test.dart
index 71ac890..928a77a 100644
--- a/fingerprint-dart-auth-sdk/test/unit/utils/fingerprint_is_valid_webhook_signature_test.dart
+++ b/fingerprint-dart-auth-sdk/test/unit/utils/fingerprint_is_valid_webhook_signature_test.dart
@@ -1,23 +1,21 @@
 import 'dart:convert';
 
-import 'package:ds_standard_features/ds_standard_features.dart';
+import 'package:crypto/crypto.dart';
 import 'package:ds_tools_testing/ds_tools_testing.dart';
 import 'package:fingerprint_dart_auth_sdk/src/utils/fingerprint_is_valid_webhook_signature.dart';
-// Update with the correct import path
 
 void main() {
   group('isValidWebhookSignature', () {
-    const String payload = '{"event": "user.login", "userId": "12345"}';
-    const String secret = 'super-secret-key';
+    const payload = '{"event": "user.login", "userId": "12345"}';
+    const secret = 'super-secret-key';
 
-    // Generate a valid signature for testing
     String generateSignature(String payload, String secret) {
       final hmac = Hmac(sha256, utf8.encode(secret));
       final digest = hmac.convert(utf8.encode(payload));
       return digest.toString();
     }
 
-    test('should return true for a valid signature', () {
+    test('returns true for a valid signature', () {
       final validSignature = generateSignature(payload, secret);
 
       final result = isValidWebhookSignature(
@@ -29,52 +27,50 @@ void main() {
       expect(result, isTrue);
     });
 
-    test('should return false for an invalid signature', () {
-      final invalidSignature = 'invalid-signature-123';
-
+    test('returns false for an invalid signature', () {
       final result = isValidWebhookSignature(
         payload: payload,
-        signature: invalidSignature,
+        signature: 'invalid-signature-123',
         secret: secret,
       );
 
       expect(result, isFalse);
     });
 
-    test('should throw ArgumentError if payload is empty', () {
+    test('throws ArgumentError if payload is empty', () {
       expect(
         () => isValidWebhookSignature(
           payload: '',
           signature: generateSignature(payload, secret),
           secret: secret,
         ),
-        throwsA(isA<ArgumentError>()),
+        throwsArgumentError,
       );
     });
 
-    test('should throw ArgumentError if signature is empty', () {
+    test('throws ArgumentError if signature is empty', () {
       expect(
         () => isValidWebhookSignature(
           payload: payload,
           signature: '',
           secret: secret,
         ),
-        throwsA(isA<ArgumentError>()),
+        throwsArgumentError,
       );
     });
 
-    test('should throw ArgumentError if secret key is empty', () {
+    test('throws ArgumentError if secret key is empty', () {
       expect(
         () => isValidWebhookSignature(
           payload: payload,
           signature: generateSignature(payload, secret),
           secret: '',
         ),
-        throwsA(isA<ArgumentError>()),
+        throwsArgumentError,
       );
     });
 
-    test('should return false if signature length is incorrect', () {
+    test('returns false if signature length is incorrect', () {
       final invalidLengthSignature = generateSignature(
         payload,
         secret,
@@ -89,7 +85,7 @@ void main() {
       expect(result, isFalse);
     });
 
-    test('should return false for completely different payloads', () {
+    test('returns false for completely different payloads', () {
       const differentPayload = '{"event": "user.logout", "userId": "54321"}';
       final validSignature = generateSignature(differentPayload, secret);
 
diff --git a/tools/pipelines/backend/child-ci-release.yml b/tools/pipelines/backend/child-ci-release.yml
index 8f22c92..a1ac6c1 100644
--- a/tools/pipelines/backend/child-ci-release.yml
+++ b/tools/pipelines/backend/child-ci-release.yml
@@ -15,10 +15,6 @@ release:
       extract_changelog_version() {
         awk '
           match($0, /^## \[([^]]+)\]/, m) { print m[1]; exit }
-          match($0, /^## \*\*([^*]+)\*\*/, m) { print m[1]; exit }
-          match($0, /^## ([^ ]+)/, m) { print m[1]; exit }
-          match($0, /^# \*\*([^*]+)\*\*/, m) { print m[1]; exit }
-          match($0, /^# ([^ ]+)/, m) { print m[1]; exit }
         ' CHANGELOG.md
       }
 
@@ -26,11 +22,7 @@ release:
         local version="$1"
         awk -v version="$version" '
           $0 ~ ("^## \\[" version "\\]") { flag=1; next }
-          $0 ~ ("^## \\*\\*" version "\\*\\*") { flag=1; next }
-          $0 ~ ("^## " version "([[:space:]]|$)") { flag=1; next }
-          $0 ~ ("^# \\*\\*" version "\\*\\*") { flag=1; next }
-          $0 ~ ("^# " version "([[:space:]]|$)") { flag=1; next }
-          flag && ($0 ~ /^## \[/ || $0 ~ /^## \*\*/ || $0 ~ /^## [0-9]/ || $0 ~ /^# \*\*/ || $0 ~ /^# [0-9]/) { exit }
+          flag && ($0 ~ /^## \[/) { exit }
           flag { print }
         ' CHANGELOG.md
       }
diff --git a/tools/pipelines/backend/child-ci-setup-validation.yml b/tools/pipelines/backend/child-ci-setup-validation.yml
index 8427803..7b6dba5 100644
--- a/tools/pipelines/backend/child-ci-setup-validation.yml
+++ b/tools/pipelines/backend/child-ci-setup-validation.yml
@@ -94,7 +94,7 @@ validate_dart_sdk_version:
     - echo "$PUBSPEC_FILES"
 
     # Define the minimum required Dart SDK version
-    - MINIMUM_VERSION="3.11.0"
+    - MINIMUM_VERSION="3.11.4"
     - ALL_VALID="true"
 
     # Iterate over each pubspec.yaml file
diff --git a/tools/pipelines/firebase-hosting/child-ci-production-docs.yml b/tools/pipelines/firebase-hosting/child-ci-production-docs.yml
index 5b19b64..2ea7afb 100644
--- a/tools/pipelines/firebase-hosting/child-ci-production-docs.yml
+++ b/tools/pipelines/firebase-hosting/child-ci-production-docs.yml
@@ -37,11 +37,11 @@ validate_branch_name:
       echo "Branch name: $BRANCH_NAME"
 
       if echo "$BRANCH_NAME" | grep -Eq "^(development|qa|main)$"; then
-        echo "✅ Valid branch name for protected branches: $BRANCH_NAME."
+        echo "âœ… Valid branch name for protected branches: $BRANCH_NAME."
       elif echo "$BRANCH_NAME" | grep -Eq "^(feat|fix|hotfix|chore|test|refactor|release|admin|docs)/[a-z0-9_-]+$"; then
-        echo "✅ Branch name follows naming convention: $BRANCH_NAME."
+        echo "âœ… Branch name follows naming convention: $BRANCH_NAME."
       else
-        echo "❌ Invalid branch name: $BRANCH_NAME."
+        echo "âŒ Invalid branch name: $BRANCH_NAME."
         echo "Expected format: <type>/<branch-name>. Valid types: feat, fix, hotfix, chore, test, refactor, release, admin, docs, development, qa, main."
         exit 1
       fi
@@ -72,7 +72,7 @@ validate_commit_messages:
       INVALID_COMMITS=$(git log -1 --format=%s | grep -vE "$COMBINED_REGEX" || true)
 
       if [ -n "$INVALID_COMMITS" ]; then
-        echo "❌ Commit message does not follow convention:"
+        echo "âŒ Commit message does not follow convention:"
         echo "$INVALID_COMMITS"
         echo "Expected formats:"
         echo "  Merge branch 'source-branch' into 'target-branch' (for merge requests)"
@@ -82,7 +82,7 @@ validate_commit_messages:
         echo "  fix(payment): resolve rounding error in total calculation"
         exit 1
       else
-        echo "✅ Commit message follows the convention."
+        echo "âœ… Commit message follows the convention."
       fi
   rules:
     - when: always
@@ -90,31 +90,33 @@ validate_commit_messages:
 firebase:preview_docs:
   image: google/cloud-sdk:alpine
   stage: preview
+  id_tokens:
+    GITLAB_OIDC_TOKEN:
+      aud: $GCP_WORKLOAD_IDENTITY_AUDIENCE_PROD
   before_script:
-    - apk add --no-cache nodejs npm 
-    - bash tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
-    - export GOOGLE_APPLICATION_CREDENTIALS="$GOOGLE_CLOUD_CREDENTIALS_PROD"
+    - apk add --no-cache nodejs npm
+    - . tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
   script:
     - ls -la
     - cd fingerprint-dart-auth-sdk/docs
-    - firebase hosting:channel:deploy $CI_COMMIT_REF_NAME --debug
+    - firebase hosting:channel:deploy "${CI_COMMIT_REF_SLUG:-$CI_COMMIT_REF_NAME}" --project "${FIREBASE_PROJECT_ID:-${GCP_PROJECT_ID_PROD:-}}" --debug
   rules:
-    # Condition 1: Branch is not main, qa, or development
     - if: '$CI_COMMIT_BRANCH != "main"'
       when: always
 
 firebase:deploy_docs:
   image: google/cloud-sdk:alpine
   stage: deploy
+  id_tokens:
+    GITLAB_OIDC_TOKEN:
+      aud: $GCP_WORKLOAD_IDENTITY_AUDIENCE_PROD
   before_script:
-    - apk add --no-cache nodejs npm 
-    - bash tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
-    - export GOOGLE_APPLICATION_CREDENTIALS="$GOOGLE_CLOUD_CREDENTIALS_PROD"
+    - apk add --no-cache nodejs npm
+    - . tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
   script:
     - ls -la
     - cd fingerprint-dart-auth-sdk/docs
-    - firebase deploy --project aortem-prod
+    - firebase deploy --project "${FIREBASE_PROJECT_ID:-${GCP_PROJECT_ID_PROD:-}}"
   rules:
-    # Condition 1: Branch is not main, qa, or development
     - if: '$CI_COMMIT_BRANCH == "main"'
       when: always
diff --git a/tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh b/tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
index 2515fd9..9f2f7c4 100644
--- a/tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
+++ b/tools/scripts/firebase/gcloud_prod_acc_firebase_production_docs.sh
@@ -1,6 +1,32 @@
 #!/bin/bash
 
-set -e
+set -euo pipefail
+
+START_DIR="$(pwd)"
+FIREBASE_PROJECT_ID="${FIREBASE_PROJECT_ID:-${GCP_PROJECT_ID_PROD:-}}"
+WIF_PROVIDER="${GCP_WORKLOAD_IDENTITY_PROVIDER_PROD:-}"
+FIREBASE_ACCOUNT="${ACCOUNT_PROD:-}"
+OIDC_TOKEN="${GITLAB_OIDC_TOKEN:-}"
+
+if [ -z "$FIREBASE_PROJECT_ID" ]; then
+  echo "ERROR: Set FIREBASE_PROJECT_ID or GCP_PROJECT_ID_PROD."
+  exit 1
+fi
+
+if [ -z "$WIF_PROVIDER" ]; then
+  echo "ERROR: Set GCP_WORKLOAD_IDENTITY_PROVIDER_PROD."
+  exit 1
+fi
+
+if [ -z "$FIREBASE_ACCOUNT" ]; then
+  echo "ERROR: Set ACCOUNT_PROD."
+  exit 1
+fi
+
+if [ -z "$OIDC_TOKEN" ]; then
+  echo "ERROR: GitLab did not provide GITLAB_OIDC_TOKEN."
+  exit 1
+fi
 
 echo "Navigating to docs directory"
 cd fingerprint-dart-auth-sdk/docs
@@ -19,25 +45,41 @@ npm run build
 
 echo "Current project: $(gcloud config get-value project)"
 
-gcloud auth activate-service-account $ACCOUNT_PROD --key-file=$GOOGLE_CLOUD_CREDENTIALS_PROD
+OIDC_TOKEN_FILE="$(mktemp)"
+WIF_CRED_FILE="$(mktemp)"
+trap 'rm -f "$OIDC_TOKEN_FILE" "$WIF_CRED_FILE"' EXIT
+
+printf '%s' "$OIDC_TOKEN" > "$OIDC_TOKEN_FILE"
 
-echo "Setting account to: $ACCOUNT_PROD"
-gcloud config set account $ACCOUNT_PROD
-gcloud config set project aortem-prod
+gcloud iam workload-identity-pools create-cred-config \
+  "$WIF_PROVIDER" \
+  --service-account="$FIREBASE_ACCOUNT" \
+  --credential-source-file="$OIDC_TOKEN_FILE" \
+  --output-file="$WIF_CRED_FILE"
+
+gcloud auth login --brief --cred-file="$WIF_CRED_FILE"
+export GOOGLE_APPLICATION_CREDENTIALS="$WIF_CRED_FILE"
+export CLOUDSDK_AUTH_CREDENTIAL_FILE_OVERRIDE="$WIF_CRED_FILE"
 
 echo "Authenticated accounts:"
 gcloud auth list
 
+gcloud config set project "$FIREBASE_PROJECT_ID"
+
 echo "Active account: $(gcloud config get-value account)"
 echo "Active project: $(gcloud config get-value project)"
 
-export GOOGLE_APPLICATION_CREDENTIALS="$GOOGLE_CLOUD_CREDENTIALS_PROD"
-
-gcloud auth activate-service-account --key-file $GOOGLE_APPLICATION_CREDENTIALS
-gcloud config set project aortem-prod
+echo "Current directory: $(pwd)"
+ls -la
 
 echo "Installing Firebase CLI globally"
 npm install -g firebase-tools
 
 echo "Listing available Firebase projects"
 firebase projects:list
+
+echo "Adding Firebase project to local configuration"
+firebase use --add "$FIREBASE_PROJECT_ID" || echo "Project already configured"
+
+echo "Returning to original working directory"
+cd "$START_DIR"
