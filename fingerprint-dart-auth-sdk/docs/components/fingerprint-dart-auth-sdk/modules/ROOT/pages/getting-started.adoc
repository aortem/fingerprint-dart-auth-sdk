= Getting Started
:page-layout: default

This page covers the quickest path to installing and navigating Fingerprint Dart Auth SDK.

== Install the Package

Add the package to your pubspec.yaml:

[source,yaml]
----
dependencies:
  fingerprint_dart_auth_sdk: ^0.0.3
----

Then run:

[source,bash]
----
dart pub get
----

== Import the Library

[source,dart]
----
import 'package:fingerprint_dart_auth_sdk/fingerprint_dart_auth_sdk.dart';
----

== Start with the Package Sources

* Pubspec: apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/pubspec.yaml
* README: apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/README.md
* Examples: apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/example
* Tests: apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/test

== Build the Docs

From apps/sdks/fingerprint-dart-auth-sdk/fingerprint-dart-auth-sdk/docs:

[source,bash]
----
npm install
npx antora antora-playbook.yml
----

The generated site is written to build/site.
